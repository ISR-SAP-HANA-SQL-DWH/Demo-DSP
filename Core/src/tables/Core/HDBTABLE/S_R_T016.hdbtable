column table "S_R_T016" (
   "HIDST_HK"  BINARY(32)  NOT NULL,
   "SINDT_LD"  TIMESTAMP  NOT NULL,
   "SINDT_SRC"  VARCHAR(100),
   "SINDT_HD"  BINARY(32),
   "MANDT"  VARCHAR(3)  NOT NULL   comment 'Client',
   "BRSCH"  VARCHAR(4)  NOT NULL   comment 'Industry',
   "BRTXT"  VARCHAR(20)     comment 'Industry Key'
, primary key  ( 
     "HIDST_HK",
     "SINDT_LD",
     "MANDT")
)
comment 'Industries'